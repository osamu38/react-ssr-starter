import React from 'react';
import { storiesOf } from '@storybook/react';
import SubTitle from 'components/SubTitle';

storiesOf('SubTitle', module).add('normal', () => (
  <SubTitle>SubTitle</SubTitle>
));
